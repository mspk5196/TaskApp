import React from 'react';
import { View, Text } from 'react-native';

const RequestsScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Requests Screen (TODO)</Text>
  </View>
);

export default RequestsScreen;
