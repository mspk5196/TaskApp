import React from 'react';
import { View, Text } from 'react-native';

const OverdueScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Overdue Screen (TODO)</Text>
  </View>
);

export default OverdueScreen;
