import React from 'react';
import { View, Text } from 'react-native';

const ReviewSubmit = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Create Task - Review & Submit (TODO)</Text>
  </View>
);

export default ReviewSubmit;
