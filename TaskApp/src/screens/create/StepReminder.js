import React from 'react';
import { View, Text } from 'react-native';

const StepReminder = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Create Task - Reminder Step (TODO)</Text>
  </View>
);

export default StepReminder;
