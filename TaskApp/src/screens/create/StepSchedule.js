import React from 'react';
import { View, Text } from 'react-native';

const StepSchedule = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Create Task - Schedule Step (TODO)</Text>
  </View>
);

export default StepSchedule;
