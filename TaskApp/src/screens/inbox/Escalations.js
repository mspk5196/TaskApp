import React from 'react';
import { View, Text } from 'react-native';

const Escalations = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Escalations (TODO)</Text>
  </View>
);

export default Escalations;
