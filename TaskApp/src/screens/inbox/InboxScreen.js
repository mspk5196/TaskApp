import React from 'react';
import { View, Text } from 'react-native';

const InboxScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Inbox Screen (TODO)</Text>
  </View>
);

export default InboxScreen;
