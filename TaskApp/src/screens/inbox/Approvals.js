import React from 'react';
import { View, Text } from 'react-native';

const Approvals = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Approvals (TODO)</Text>
  </View>
);

export default Approvals;
