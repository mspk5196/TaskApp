import React from 'react';
import { View, Text } from 'react-native';

const CalendarScreen = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Calendar Screen (TODO)</Text>
  </View>
);

export default CalendarScreen;
